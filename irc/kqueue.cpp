#include <sys/event.h>
#include <sys/time.h>
#include <sys/types.h>
#include <iostream>

int main()
{
	// int fd = kqueue();
	// if (fd == -1)
	// {
	// 	std::cout << "event queue creation failed" << std::endl;
	// 	return 0;
	// }
	// struct kevent events[10];
	// EV_SET(newEventFd, fd, EV, flags, fflags, data, udata);
	// kevent(fd, )
	return 0;
}